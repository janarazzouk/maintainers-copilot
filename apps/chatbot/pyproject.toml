[project]
name = "maintainers-copilot-chatbot"
version = "0.1.0"
description = "Streamlit chatbot and admin UI for Maintainer's Copilot"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.40.0",
    "requests>=2.32.0",
]

[tool.uv]
package = false