[project]
name = "maintainers-copilot-common"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = [
    "pydantic>=2.8.0",
    "pydantic-settings>=2.4.0",
    "structlog>=24.0.0"
]

[tool.setuptools.packages.find]
where = ["src"]